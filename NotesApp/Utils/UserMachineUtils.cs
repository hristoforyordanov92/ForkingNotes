namespace NotesApp.Utils
{
    public static class UserMachineUtils
    {
        public static void GetUserMachineResolution()
        {
            // todo: eventually get around to validate if the default settings
            // or the previously set by the user settings are good.
            // and if not - make the window smaller.
        }
    }
}
