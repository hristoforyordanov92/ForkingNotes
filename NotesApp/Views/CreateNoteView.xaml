<Window x:Class="NotesApp.Views.CreateNoteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:NotesApp.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:CreateNoteViewModel}"
        mc:Ignorable="d"
        Title="New Note" Height="450" Width="400">
    <Grid Margin="4 12 4 4">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" Text="Name" />
            <TextBox Grid.Column="1" Grid.Row="0" Margin="4 0 0 4" Text="{Binding Name}" />

            <TextBlock Grid.Column="0" Grid.Row="1" Text="Tags" />
            <TextBox Grid.Column="1" Grid.Row="1" Margin="4 0 0 4" Text="{Binding Tags}" />
        </Grid>

        <TextBlock Grid.Row="1" Text="Content" />
        <TextBox Grid.Row="2" Margin="0 0 0 4" Text="{Binding Content}" />
        <Button Grid.Row="3" Content="Create Note" Command="{Binding CreateNoteCommand}" />
    </Grid>
</Window>
